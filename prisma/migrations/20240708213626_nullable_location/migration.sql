BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Property] ALTER COLUMN [location_lat] DECIMAL(9,6) NULL;
ALTER TABLE [dbo].[Property] ALTER COLUMN [location_lon] DECIMAL(9,6) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
